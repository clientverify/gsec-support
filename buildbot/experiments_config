#experiments.sh : config file for buildbot

# note: list variables with the equivalent prefixes should have
# equivalent number of elements


# list of modes to run cliver.sh
EXPERIMENT_LIST=(naive)

# list of output tags
EXPERIMENT_LIST_OUTPUT=(naive)

# list of client types
CLIENT_LIST=(openssl-opt-klee xpilot-ng-x11-klee)

# location of directories holding ktest files for experiments
CLIENT_LIST_KTEST=(
/playpen/buildbot/data/network/openssl/gmail-subset
/playpen/buildbot/data/network/xpilot-ng-x11/xpilot_2_100
)

# list of extra paramters specific to each client type
CLIENT_LIST_EXTRA_PARAMETERS=("--use-threads=1" "--use-threads=1")

# data locations
RESULTS_LOCATION="data/results"
